from deep_translator import GoogleTranslator

print("CodeAlpha Task 1 - Language Translation Tool")

while True:
    text = input("\nEnter English text: ")
    if text.lower() == "exit":
        break
    result = GoogleTranslator(source='en', target='hi').translate(text)
    print("Hindi:", result)